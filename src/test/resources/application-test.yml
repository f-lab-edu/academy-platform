spring:
  session:
    storeType: none



logging:
  config: classpath:log4j2-spring.xml
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql:
              BasicBinder: off
        sql: off
    p6spy: debug

decorator:
  datasource:
    p6spy:
      enable-logging: true

